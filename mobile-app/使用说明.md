# 终端任务监控 · 移动端 App

基于 Capacitor 的原生外壳，加载线上响应式前台
（<https://monitor.vita-llm.com/portal>），包含前台**全部功能**：会话列表、
Claude Code 终端式对话流、暂停/中断/终止、发布任务、设备管理、设置等。
前台已做手机自适应（侧栏抽屉、单列、触摸友好）。

## Android（已打好安装包，开箱即用）

`dist/终端任务监控-android.apk` —— 直接装到任意 Android 手机：

1. 把 APK 传到手机（微信/网盘/数据线均可）。
2. 点开安装；首次会提示「允许安装未知来源应用」，允许即可。
3. 打开「终端任务监控」App，登录后即可使用全部前台功能。

> 这是 debug 签名包，个人/内部分发直接可用。要上架 Google Play 需用正式签名
> 重新打 release 包（`./gradlew assembleRelease` + 签名配置）。

## iOS（已打好 .ipa，侧载安装）

`dist/终端任务监控-ios.ipa` —— 无签名安装包，用你自己的 Apple ID 免费签名后装到自己的 iPhone：

### 方式一：Sideloadly（推荐，最省事）

1. 在 Mac 上装 [Sideloadly](https://sideloadly.io/)；
2. iPhone 用数据线连 Mac（首次点「信任此电脑」）；
3. 打开 Sideloadly，把 `终端任务监控-ios.ipa` 拖进去，填你的 Apple ID → Start；
4. 手机上「设置 → 通用 → VPN与设备管理」信任你的开发者证书；
5. 免费签名 7 天有效，到期重新签一次即可（或用 AltStore 自动续签）。

### 方式二：Xcode 直装（这台 Mac 就能操作）

1. iPhone 连 Mac，手机开「开发者模式」（设置 → 隐私与安全性）；
2. 打开 `mobile-app/ios/App/App.xcworkspace`；
3. Signing & Capabilities 里 Team 选你的 Apple ID（Personal Team）；
4. 顶部设备选你的 iPhone，⌘R 运行即安装。

> 上架 App Store 需 $99/年开发者账号；个人自用侧载零成本。
> 另：Safari 打开 <https://monitor.vita-llm.com/portal> → 分享 → 添加到主屏幕，
> 可获得免签名的 PWA 版（无 7 天限制，体验接近原生）。

## 工程说明

- 框架：Capacitor 7（`capacitor.config.json`）
- 加载方式：`server.url` 指向线上前台，App 始终显示最新界面，改前端无需重打包。
- 应用 ID：`com.vitahsu.agentmonitor`，名称：终端任务监控。

## 重新构建

```bash
cd mobile-app
npx cap sync                       # 同步配置到原生工程

# Android APK
cd android && ./gradlew assembleDebug
#   产物：android/app/build/outputs/apk/debug/app-debug.apk
#   需要 JDK 21 + Android SDK（ANDROID_HOME）

# iOS（在 Mac 上）
npx cap open ios                   # 打开 Xcode 后编译
```

## 与桌面客户端的区别

- **桌面客户端**（`clients/`）：跑在电脑上，负责**上报**本机 Claude 会话。
- **移动端 App**（本目录）：跑在手机上，负责**查看与控制**——它不上报会话，
  是前台的原生外壳。两者配合：电脑装客户端上报，手机装 App 随时随地看。
