# 生产构建用的环境变量模板。
#
# 真实的 .env.prod **不入库**（值由部署方持有），本文件是它的唯一结构真源：
#   - 本地出生产包：cp .env.prod.example .env.prod，再把下面两个密钥换成生产值；
#   - CI：由 scripts/write-env-prod.sh 读本文件、用仓库变量注入两个密钥后生成 .env.prod。
#
# 说明一下这两个值为什么不是「机密」：CRYPTO_KEY 会被 webpack 内联进前端 bundle，
# 打开线上站点扒 JS 就能拿到；RSA_PUB_KEY 本来就是公钥。它们只是「部署相关配置」，
# 必须与 hub 的 AM_CRYPTO_KEY / RSA 私钥配对，不配对登录会「无请求即失败」。
NODE_ENV=production
API_BASE=''
DEFAULT_PATH="/portal"
# 登录加密（与 agent-task-monitor 的 AM_CRYPTO_KEY / RSA 私钥配对）
CRYPTO_KEY=__CRYPTO_KEY__
RSA_PUB_KEY=__RSA_PUB_KEY__
